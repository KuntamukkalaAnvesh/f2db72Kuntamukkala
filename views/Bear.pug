extends layout
block content
  h1= title
  -
  -let result = results 
  div.container-fluid
     div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) Bear_Name: #{item["Bear_name"]}  <br>  Bear_size: #{item["Bear_size"]} <br> Bear_type: #{item["Bear_type"]} 
  script.
      function buy(index){
         alert("you purchased item "+(index+1))
      }